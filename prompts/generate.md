次の企画を、fantasy-msx の作品として実装してください。

## 作品の型

{{form}}

## 企画

{{plan}}

企画の `structure`（段取り）と縛り（`twist`）は、守るべき指示です。そこに書いてある
画面の並びと進み方のとおりに組んでください。題材だけを取って、段取りを別のものに
すり替えてはいけません。

## 実例

同じ型の作品の例です。**これは API の使い方と、毎フレームの仕事を少なく保つやり方を
見るためのものです。設計図ではありません。**データの中身（地図、絵、文章、MML、場面の
一覧）はわざと省いてあります。そこは企画に合わせて自分で作ってください。

関数の分け方、状態の持ち方、画面の並び、進み方は、この例に合わせる必要はありません。
企画の段取りに合う形を自分で組んでください。例と同じ順に同じ関数が並ぶ作品になったら、
それは企画を実装していません。

```ts
{{template}}
```

### 例のものなので使わないもの

{{avoid}}

同じ役の処理が必要なら、自分の名前で、自分の中身で書いてください。

## 出力

`work.ts` と `meta.json` の全文を、次の形のコードブロックで返してください。ほかのファイルは作れません。

```ts work.ts
（work.ts の全文）
```

```json meta.json
（meta.json の全文）
```
